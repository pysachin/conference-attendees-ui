import { ShowFcErrorDirective } from './show-fc-error.directive';

describe('ShowFcErrorDirective', () => {
  it('should create an instance', () => {
    const directive = new ShowFcErrorDirective();
    expect(directive).toBeTruthy();
  });
});
